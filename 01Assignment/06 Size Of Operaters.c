#include<stdio.h>
#include<conio.h>

int main()
{
    double no=0;
    int Num=0;

    printf("\n Size Of Variables ==>  ");

    printf("\n %d Byte", sizeof(char));
    printf("\n %d Bytes", sizeof(int));
    printf("\n %d Bytes", sizeof(float));
    printf("\n %d Bytes", sizeof(double));
    printf("\n %d Bytes", sizeof(Num));

    getch();
    return 0;
}
